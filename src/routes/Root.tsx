import Layout from "../components/common/layout/Layout";
import Home from "../pages/home/Home";

const Root = () => {
  return (
    <Layout>
        <Home />
    </Layout>
  );
};

export default Root;
