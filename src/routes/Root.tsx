import Layout from "../components/common/layout/Layout";
import Tasks from "../pages/tasks/Tasks";

const Root = () => {
  return (
    <Layout>
      <Tasks />
    </Layout>
  );
};

export default Root;
